#include <stdio.h>
#include "ft_list.h"

extern int	list_size(t_list *begin_list);

int main(void)
{
	t_list *head;
	t_list *node1;
	t_list *node2;

	

	head->next = node1;
	node1->next = node2;
	node2->next = NULL;

	return (0);
}
